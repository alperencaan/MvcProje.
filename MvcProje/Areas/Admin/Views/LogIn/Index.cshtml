<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Exo', sans-serif;
            background: #1d1f2e;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow: hidden;
        }

        .container {
            position: relative;
            width: 400px;
            background-color: rgba(0, 0, 0, 0.6);
            border-radius: 10px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }

        .header {
            text-align: center;
            color: #fff;
            font-size: 28px;
            margin-bottom: 30px;
            font-weight: 300;
        }

            .header span {
                color: #00bcd4;
                font-weight: 700;
            }

        .login input[type="text"],
        .login input[type="password"] {
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            background: transparent;
            border: 2px solid #00bcd4;
            border-radius: 5px;
            color: #fff;
            font-size: 16px;
            outline: none;
            transition: border 0.3s;
        }

            .login input[type="text"]:focus,
            .login input[type="password"]:focus {
                border-color: #4caf50;
            }

        .login input[type="button"] {
            width: 100%;
            padding: 15px;
            background: #00bcd4;
            border: none;
            border-radius: 5px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s;
        }

            .login input[type="button"]:hover {
                background: #0097a7;
            }

            .login input[type="button"]:active {
                background: #007c91;
            }

            .login input[type="button"]:focus {
                outline: none;
            }

        .background-image {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: url('https://images.unsplash.com/photo-1506748686217-3f055d496010'); /* Background image */
            background-size: cover;
            filter: blur(8px);
            z-index: -1;
        }

        .login-form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

            .login-form a {
                text-align: center;
                margin-top: 15px;
                color: #fff;
                text-decoration: none;
                font-size: 14px;
            }

                .login-form a:hover {
                    color: #FFA500;
                }

    </style>
</head>
<body>
    <div class="background-image"></div>
    <div class="container">
        <div class="header">
            <div>BLOG<span>CV</span></div>
        </div>
        <div class="login-form">
            <input type="text" placeholder="Username" name="user"><br>
            <input type="password" placeholder="Password" name="password"><br>
            <input type="button" value="Login">
            <a href="#">Forgot Password?</a>
        </div>
    </div>
</body>
</html>
