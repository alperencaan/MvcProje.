<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Page</title>
    <style>
        :root {
            --main-orange: #ff7300;
            --main-orange-dark: #e65c00;
            --main-orange-light: #ffb380;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Exo', sans-serif;
            background: #1d1f2e;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            overflow: hidden;
            position: relative;
        }

        .background-image {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: url('https://images.unsplash.com/photo-1506748686217-3f055d496010');
            background-size: cover;
            background-position: center;
            filter: blur(8px);
            z-index: -1;
        }

        .container {
            width: 400px;
            background-color: rgba(0, 0, 0, 0.7);
            border-radius: 10px;
            padding: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }

        .header {
            text-align: center;
            font-size: 28px;
            margin-bottom: 30px;
            color: var(--main-orange-light);
            font-weight: 300;
        }

            .header span {
                color: var(--main-orange);
                font-weight: 700;
            }

        .login-form input[type="text"],
        .login-form input[type="password"] {
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            background: transparent;
            border: 2px solid var(--main-orange);
            border-radius: 5px;
            color: #fff;
            font-size: 16px;
            outline: none;
            transition: border 0.3s;
        }

            .login-form input[type="text"]:focus,
            .login-form input[type="password"]:focus {
                border-color: var(--main-orange-light);
            }

        .login-form input[type="submit"] {
            width: 100%;
            padding: 15px;
            background: var(--main-orange);
            border: none;
            border-radius: 5px;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s;
        }

            .login-form input[type="submit"]:hover {
                background: var(--main-orange-dark);
            }

        .login-form a {
            display: block;
            text-align: center;
            margin-top: 15px;
            color: #fff;
            text-decoration: none;
            font-size: 14px;
            transition: color 0.3s;
        }

            .login-form a:hover {
                color: var(--main-orange-light);
            }
    </style>
</head>
<body>
    <div class="background-image"></div>
    <div class="container">
        <div class="header">
            BLOG<span>CV</span>
        </div>
        <div class="login-form">
            <form action="/your-login-url" method="post">
                <input type="text" placeholder="Username" name="user" required />
                <input type="password" placeholder="Password" name="password" required />
                <input type="submit" value="Login" />
            </form>
            <a href="#">Forgot Password?</a>
        </div>
    </div>
</body>
</html>
